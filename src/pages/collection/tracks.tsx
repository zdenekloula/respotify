import { MainLayout } from '@/components/layout/MainLayout';

function CollectionTracks() {
  return (
    <MainLayout>
      <p>Hello from collection tracks :)</p>
    </MainLayout>
  );
}

// eslint-disable-next-line import/no-default-export
export default CollectionTracks;
